---
import ToolLayout from '@/layouts/ToolLayout.astro';
import PdfToPngTool from '@/components/pdf/pdf-to-png/PdfToPngTool';
import ToolShell from '@/components/ToolShell.astro';
import { ALL_TOOLS_SEO } from '@/lib/seoData';
import { getOgImagePath } from '@/lib/ogImages';

const toolId = 'pdf-to-png';
const seo = ALL_TOOLS_SEO[toolId as keyof typeof ALL_TOOLS_SEO];
---
<ToolLayout title={seo?.title ?? 'itsmypdf Tool'} description={seo?.description ?? 'PDF tool'} canonical={'/pdf-to-png'} ogImage={getOgImagePath(toolId)}>
  <PdfToPngTool client:only="react">
    <ToolShell slot="fallback" title={seo?.h1 ?? seo?.title ?? 'PDF Tool'} description={seo?.description ?? 'PDF tool'} />
  </PdfToPngTool>
</ToolLayout>
